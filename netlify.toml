# Zaanisung frontend — Netlify build configuration.
# The production build points at the real backend. Configure the backend
# origin via VITE_API_URL / VITE_API_BASE_URL in the Netlify environment
# (e.g. https://api.zaanisung.com) when the API is hosted separately.
[build]
  command = "npm run build"
  publish = "dist"